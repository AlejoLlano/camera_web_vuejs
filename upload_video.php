
<!-- public function upload_video_inspection(Request $request){
    
    $file = $request->file('video');
    $ruta = public_path('videos/video_inspections');
    $fileName = uniqid(). ".mp4";
    $file->move($ruta, $fileName);
    
} -->